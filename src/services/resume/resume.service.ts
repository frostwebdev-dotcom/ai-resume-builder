import "server-only";

/**
 * Resume CRUD, preview data, and PDF export orchestration.
 * Persist to Supabase and gate PDF download on payment status in later work.
 */
export const resumeService = {
  // placeholder for domain methods
} as const;
