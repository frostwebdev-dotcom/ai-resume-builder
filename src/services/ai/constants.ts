/**
 * Bump when prompt copy or structure changes materially (observability + rollback).
 * @deprecated Prefer PROMPT_REGISTRY[op].version per operation when diverging.
 */
export const GLOBAL_PROMPT_VERSION = "2026.04.15-v1";
