/**
 * Full-viewport studio shell (guest `/create` and signed-in project draft).
 * Keep in sync with usage in `src/app/(studio)/layout.tsx`.
 */
export const STUDIO_VIEWPORT_ROOT_CLASS =
  "fixed inset-0 flex h-[100dvh] w-screen flex-col overflow-hidden overscroll-none bg-slate-50";
