/**
 * Shared template-picker microcopy: guest studio strip + signed-in Preview & export gallery.
 * Keeps both surfaces feeling like one feature (layout swap only; content unchanged).
 */
export const TEMPLATE_PICKER_TITLE = "Template";

/** Single caption — avoids stacking two similar sentences in the UI. */
export const TEMPLATE_PICKER_CAPTION =
  "Switch layouts anytime; your wording stays.";
