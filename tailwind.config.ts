import type { Config } from "tailwindcss";

/**
 * Tailwind CSS v4 is primarily configured in `src/app/globals.css` (`@import "tailwindcss"`, `@theme`).
 * This file exists for tooling compatibility and any future JS-based extensions.
 */
const config = {} satisfies Config;

export default config;
